# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.tsbuildinfo

# scrape cache — large, local-only
data/scrape-cache.json

# env — never commit real keys
.env
.env.local
.env*.local
